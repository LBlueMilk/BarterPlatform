@model IEnumerable<BarterPlatform.Models.AdministrativeRegion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdminRegion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdminRegion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdmID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdmID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdmID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
